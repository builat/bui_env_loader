# Values read immediately while AppConfig is constructed.
APP_HOST=127.0.0.1
APP_PORT=8080

# Optional: remove this line to see Notify and Strict materialization modes.
APP_TOKEN=development-token

# Read again whenever Lazy::get() is called.
APP_LOG_LEVEL=debug

